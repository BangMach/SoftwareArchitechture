version: "3.3"
services:
  discovery-service:
    container_name: discovery-service
    build:
      context: .
      dockerfile: Dockerfile
    image: docker-discovery-service
    ports:
      - 8761:8761
    networks:
      - restaurant
    restart: always
networks:
  restaurant:
    name: restaurant
    driver: bridge