version: "3.3"
services:
  reservation-service:
    container_name: reservation-service
    build:
      context: .
      dockerfile: Dockerfile
    image: docker-reservation-service
    ports:
      - 8086:8086
    networks:
      - restaurant
#    restart: always
networks:
  restaurant:
    name: restaurant
    driver: bridge